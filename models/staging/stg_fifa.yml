version: 2

models:
  - name: stg_player
    description: this is a staging model for player
    columns:
      - name: age
        tests:
          - not_null
      - name: player_name
        tests:
          - not_null
      - name: player_id
        description: This is the primary key
  - name: event

  - name: stg_event_type
    description: ""
    columns:
      - name: event_type_id
        tests:
          - unique
          - not_null
  

  - name: stg_team
    description: ""
    columns:
      - name: team_id
        tests:
          - unique
          - not_null