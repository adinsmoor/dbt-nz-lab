version: 2

models:
  - name: event

  - name: stg_event_type
    description: "Event type metadata"
    columns:
      - name: event_type_id
        tests:
          - unique
          - not_null
  

  - name: stg_team
    description: "Team metadata"
    columns:
      - name: team_id
        tests:
          - unique
          - not_null